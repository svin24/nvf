{
  config.vim.assistant.copilot = {
    enable = true;
    mappings.suggestion = {
      dismiss = "<C-\\>";
    };

    # panel
    setupOpts.panel.enabled = false;
    mappings.panel = { };
  };
}
